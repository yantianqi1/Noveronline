# AGENTS.md

## 目的

本文件用于说明 `MiroFish-Novel` 在后续 Codex 开发过程中，与原项目 `MiroFish` 的参考关系。

## 上游参考项目

- 原项目名称：`MiroFish`
- 本地路径：`/Users/项目/MiroFish`
- 开源地址：`https://github.com/666ghj/MiroFish`

## 开发约定

后续在本仓库进行开发时，Codex 可以参考原项目 `MiroFish` 的设计、结构和实现方式，尤其可以优先借鉴以下内容：

- 文件上传与文本解析链路
- 项目持久化与任务状态管理
- 图谱构建服务
- Zep 实体读取与相关工具封装
- LLM 调用封装
- 前后端工作流组织方式

## 借鉴边界

参考 `MiroFish` 时，默认遵循以下原则：

- 优先复用“底层能力”和“工程实现思路”，不要机械照搬业务语义。
- 当前仓库的产品目标是“小说分析、角色关系演化、世界线推演”，不是“舆情/社交媒体模拟平台”。
- 原项目中与 `Twitter`、`Reddit`、舆情传播、社媒平台指标强绑定的部分，只能按需拆解参考，不能直接作为当前项目默认方案。
- 如果需要迁移原项目代码，应先检查当前仓库是否已经有对应实现，避免重复造轮子或把旧语义重新带回本项目。
- 如果某项实现来自原项目，优先做“小说语义适配”后再合入当前项目。

## 建议参考方式

- 先查当前仓库已有实现，再对照原项目对应模块。
- 优先对照服务层、模型层、接口层，不要先从 UI 文案和展示层照搬。
- 如果原项目存在更成熟的实现，可把它作为“参考样板”，但最终输出必须以本仓库当前定位为准。

## 与其他 Agent 文档的关系

本文件只是当前项目根目录下的说明文档，用于补充说明参考来源与开发边界。

- 它不会覆盖其他路径下的文档文件。
- 它不会自动替换例如 `.codex/AGENTS.md`、`./codex/AGENTS.md` 或其他目录中的同名/类似文件。
- 不同路径下的文档是彼此独立的文件；是否被工具读取，取决于工具本身的查找规则，而不是本文件会去“覆盖”它们。

## 备注

如果后续需要在文档中引用原项目，请统一使用以下信息：

- 本地路径：`/Users/项目/MiroFish`
- GitHub：`https://github.com/666ghj/MiroFish`

前端启动端口：3891
后端启动端口：5101
每次超过 50 行的代码改动后，都需要完整的重新启动项目